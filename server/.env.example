PORT=5100
CLIENT_URL=http://localhost:5300
# Leave MONGODB_URI empty to use an in-memory MongoDB for development
# (data is persisted in the .mongo-data folder across restarts).
MONGODB_URI=
JWT_SECRET=change-this-secret-in-production
JWT_EXPIRES_IN=12h
NODE_ENV=development